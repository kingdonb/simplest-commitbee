apiVersion: v1
kind: Service
metadata:
  name: {{ include "marxc.fullname" . }}
  labels:
    {{- include "marxc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marxc.selectorLabels" . | nindent 4 }}

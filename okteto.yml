name: simplest-commitbee

# The build section defines how to build the images of your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#build
build:

  # You can use the following env vars to refer to this image in your deploy commands:
  #  - OKTETO_BUILD_SIMPLEST-COMMITBEE_REGISTRY: image registry
  #  - OKTETO_BUILD_SIMPLEST-COMMITBEE_REPOSITORY: image repo
  #  - OKTETO_BUILD_SIMPLEST-COMMITBEE_IMAGE: image name
  #  - OKTETO_BUILD_SIMPLEST-COMMITBEE_TAG: image tag
  simplest-commitbee:
    context: .
    dockerfile: Dockerfile
    image: kingdonb/simplest-commitbee:okteto
    export_cache: kingdonb/simplest-commitbee:okteto
    cache_from:
      - kingdonb/simplest-commitbee:okteto

# The deploy section defines how to deploy your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#deploy
# deploy:
#   commands:
#   - name: Deploy
#     command: echo 'Replace this line with the proper 'helm' or 'kubectl' commands to deploy your development environment'


# The dependencies section defines other git repositories to be deployed as part of your development environment
# More info: https://www.okteto.com/docs/reference/manifest/#dependencies
# dependencies:
#   - https://github.com/okteto/sample


# The dev section defines how to activate a development container
# More info: https://www.okteto.com/docs/reference/manifest/#dev
dev:
  simplest-commitbee:
    image: kingdonb/simplest-commitbee:okteto
    command: bash --login
    environment:
      BEEMINDER_USERNAME: YOUR_BEEMINDER_USERNAME
      BEE_AUTH_TOKEN: 1234567890abcdefghij
    securityContext:
      runAsUser: 999
      runAsGroup: 1000
      fsGroup: 1000
      capabilities:
        add:
          - SYS_PTRACE
    sync:
      - .:/home/rvm/simplest-commitbee
    forward:
      - 1234:1234
      - 8080:8080
    autocreate: true
    volumes:
      - /usr/local/bundle/cache

